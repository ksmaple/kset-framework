# CAT Docker Compose 项目名
COMPOSE_PROJECT_NAME=kset-cat

# CAT 控制台 HTTP 端口（宿主机映射，默认 8088，避免与 demo 8080 冲突）
CAT_HTTP_PORT=8088

# CAT 客户端上报 TCP 端口
CAT_TCP_PORT=2280

# CAT 内置 MySQL 宿主机映射端口（默认 3307，避免与本地 MySQL 3306 冲突）
CAT_MYSQL_PORT=3307

# MySQL 镜像
CAT_MYSQL_IMAGE=mysql:5.7.37
